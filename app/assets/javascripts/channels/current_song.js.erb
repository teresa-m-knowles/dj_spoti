// MakePartyChannel = function(partyIdentifier) {
  App.current_song = App.cable.subscriptions.create({channel: 'CurrentSongChannel', party: current_party_identifier}, {

    connected: function() {
      console.log('Got here. Connected');
      console.log('Party identifier is ' + current_party_identifier);
    },

    disconnected: function() {
      console.log('disconnected');
    },

    received: function(data) {
      console.log("Receiving function with " + data.song);
      console.log(data.party);
      $('#currently-playing-song').html(data.song);
   },
  });